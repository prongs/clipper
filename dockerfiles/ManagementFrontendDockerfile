ARG CODE_VERSION
FROM clipper/frontend_base:${CODE_VERSION}

# Build Clipper

RUN cd /clipper/release && make management_frontend

ENTRYPOINT ["/clipper/release/src/management/management_frontend"]

# vim: set filetype=dockerfile:

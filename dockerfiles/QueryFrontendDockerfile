ARG CODE_VERSION
FROM clipper/frontend_base:${CODE_VERSION}

# Build Clipper

RUN cd /clipper/release && make query_frontend

ENTRYPOINT ["/clipper/release/src/frontends/query_frontend"]

# vim: set filetype=dockerfile:
